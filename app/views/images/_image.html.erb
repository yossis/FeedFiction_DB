<div class="thumb">
  <% if image.image_thumb? && image.image_processed %>
  <%= link_to image_tag(image.image_thumb_url(:thumb)) %>
  <%else%>
  <%= link_to image_tag(image.image_source) %>
  <%end%>
</div>