$('#tiles').append("<%= escape_javascript(render(:partial => 'stories/story' , :collection => @stories))%>")
<% if @stories.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@stories) %>');
<% else %>
$('.pagination').remove();
<% end %>
<% sleep 1%>
feedfiction.actions.renderBoxes();
