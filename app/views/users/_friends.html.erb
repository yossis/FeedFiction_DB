<% if current_user && @friends && @friends.size > 0%>
<div class="span4 portfolio-item" id="friends-to-follow" >
		<h2>Feeders to follow</h2>
		<ul>
			<%= render :partial => 'users/user' , :collection => @friends, :as => :user %>
		</ul>
		<div class="see-all"><%= link_to 'See All' , friends_index_url%></div>
</div>
<%end%>