<% if current_user?(@user) %>

		<%= link_to "That's you", '#', class: "btn btn-small thats-you disabled" %>
<%else%>
  <% if current_user.following?(@user) %>
    <%= render 'users/unfollow' %>
  <% else %>
    <%= render 'users/follow' %>
  <% end %>
<% end %>