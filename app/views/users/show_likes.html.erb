<% provide(:title, @title) %>
<section id="user-header">
  <%= render 'user_header' %>
</section>
<section>
	<% if @stories.count>0 %>
	<h2><%= @user.name %> likes the stories:</h2>
	<%= render 'feeds/feed' %>
	<%else%>
	<h2><%= @user.name %> dosen't like yet</h2>
	<%end%>
</section>