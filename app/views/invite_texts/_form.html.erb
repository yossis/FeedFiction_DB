<%= form_for(@invite_text) do |f| %>
  <% if @invite_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invite_text.errors.count, "error") %> prohibited this invite_text from being saved:</h2>

      <ul>
      <% @invite_text.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :story_type %><br />
    <%= f.number_field :story_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
